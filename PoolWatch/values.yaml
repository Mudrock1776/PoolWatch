namespace: poolwatch

poolwatch:
  image: repo.skadi/poolwatch:0.3.1
  hostname: test.skadi
  deviceHostname: device.skadi
  database: mongodb://mongo-production-service.poolwatch.svc.cluster.local
  tlsSecret: skaditls-secret

mongodb:
  image: repo.skadi/mongo:6
  storage: 2Gi

emailSecret: emailcred